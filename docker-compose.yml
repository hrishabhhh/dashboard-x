services:
  backend:
    build:
      context: ./backend
    container_name: dashboard-backend
    env_file:
      - ./backend/.env
    ports:
      - "5000:5000"

  frontend:
    build:
      context: ./frontend
    container_name: dashboard-frontend
    ports:
      - "3000:80"
